# 顶刊文献追踪技能 — Python 依赖
# 安装: pip install -r requirements.txt

requests>=2.28.0          # PubMed API 请求
numpy>=1.24.0             # 数值计算
matplotlib>=3.7.0         # 图表生成 (可选，用于生成PNG图表)
wordcloud>=1.9.0          # 词云生成 (可选)
flask>=2.3.0              # Web UI
